apiVersion: v1
kind: Service
metadata:
  name: enterprise-api
  labels:
    app: enterprise-api
    component: api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: enterprise-api
    component: api
  sessionAffinity: None
